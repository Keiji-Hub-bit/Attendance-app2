<% provide(:title, "login") %>
  <h1>ログイン</h1>
  
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_with(model: @user, local: true) do |f| %>
      
        <%= f.label :email, class: "label-signup" %>
        <%= f.email_field :email, class: "form-control" %>
        
        <%= f.label :password, class: "label-signup" %>
        <%= f.password_field :password, class: "form-control" %>
        
         <!--check_box:この場合、remember_meが登録先のカラム名-->
        <label class="checkbox inline checkbox-login" for="remember_me">
        <!--inline:同じ水平行にチェックボックスをグループ化する-->
        <input name="session[remember_me]" type="hidden" value="0"><input id="remember-me" type="checkbox" value="1" name="session[remember_me]">
        <span>ログイン状態を記録する</span>
                <!--span:囲った部分をインライン要素としてグループ化することが出来る-->
</label>
        
        <%= f.submit "ログイン" , class: "btn btn-primary btn-block btn-signup" %>
       <% end %>
         <p>アカウント作成は <%=link_to "こちら" , login_path %>から</p>
    </div>
  </div>